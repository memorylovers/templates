
describe("index.test", () => {
  beforeAll(async () => {
  });

  afterAll(async () => {
  });

  // ********************************************************
  // * 
  // ********************************************************
  test("index.test.1", async () => {
    expect(1).toBe(1);
  });
});